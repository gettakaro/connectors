{
  "name": "VEIN",
  "tagline": "Native connector for a VEIN Linux dedicated server.",
  "assetMode": "all",
  "requirements": [
    "Takaro registration token",
    "Control of the VEIN Linux dedicated server startup command"
  ],
  "assets": [
    {
      "pattern": "takaro-vein-plugin.tar.gz",
      "label": "Plugin",
      "requires": "VEIN Linux dedicated server",
      "hint": "Load with LD_PRELOAD on the game binary's launch line only; the archive includes configuration, licenses and upgrade instructions.",
      "required": true
    }
  ]
}
